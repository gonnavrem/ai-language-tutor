import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.gonnavrem.languagetutor',
  appName: 'Language Tutor',
  webDir: 'dist',
}

export default config
